import * as React from "react";
import styles from "./go-to-top.module.sass";

const GoToTop = props => {
  return(
    <div className={styles.container}>
      Top
    </div>
  );
};

export default GoToTop;
