# Gmail account used to send contact-form emails (requires a Google App Password, not the login password)
GMAIL_USER=
GMAIL_APP_PASSWORD=

# Optional — recipient of contact-form emails. Defaults to zyadyasser6@gmail.com if unset.
CONTACT_TO_EMAIL=
