import * as React from "react";
const styles = require("./freelance-projects.component.sass");

const FreelanceProjects = props => {
  return(
    <div className={styles.container}>
      Top
    </div>
  );
};

export default FreelanceProjects;
