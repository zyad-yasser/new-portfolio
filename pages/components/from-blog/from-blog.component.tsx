import * as React from "react";
const styles = require("./from-blog.component.sass");

const FromBlog = props => {
  return(
    <div className={styles.container}>
      Top
    </div>
  );
};

export default FromBlog;
