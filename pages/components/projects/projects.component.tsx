import * as React from "react";
const styles = require("./projects.component.sass");

const Projects = props => {
  return(
    <div className={styles.container}>
      Top
    </div>
  );
};

export default Projects;
