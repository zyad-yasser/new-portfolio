import * as React from "react";
const styles = require("./testimonials.component.sass");

const TestimonialCard = (props) => {
  return(
    <div className={`d-flex align-items-center justify-content-center w-100 p-4 ${styles.card}`}>
      
    </div>
  );
};

export default TestimonialCard;
