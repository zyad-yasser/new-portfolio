import * as React from "react";
const styles = require("./social-media.component.sass");

const SocialMedia = props => {
  return(
    <div className={styles.container}>
      Top
    </div>
  );
};

export default SocialMedia;
