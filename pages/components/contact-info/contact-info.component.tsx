import * as React from "react";
const styles = require("./contact-info.component.sass");

const ContactInfo = props => {
  return(
    <div className={styles.container}>
      Top
    </div>
  );
};

export default ContactInfo;
