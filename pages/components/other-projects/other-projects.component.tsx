import * as React from "react";
const styles = require("./other-projects.component.sass");

const OtherProjects = props => {
  return(
    <div className={styles.container}>
      other
    </div>
  );
};

export default OtherProjects;
