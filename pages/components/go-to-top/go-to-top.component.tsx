import * as React from "react";
const styles = require("./go-to-top.component.sass");

const GoToTop = props => {
  return(
    <div className={styles.container}>
      Top
    </div>
  );
};

export default GoToTop;
