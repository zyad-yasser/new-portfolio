import * as React from 'react';
import { useEffect, useState } from 'react';
const styles = require("./project-card.component.sass");

const ProjectCard = ({ project }) => {
  return (<></>);
}

export default Tabs;